export const configFun = (accessKey, secretKey, region) => {
  return {
    // basePath: '/plugins/xformation-workflow-engine',
    ACCESS_KEY_ID: accessKey,
    SECRET_KEY: secretKey,
    REGION: region,
  };
};
